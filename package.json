{
  "type": "module",
  "scripts": {
    "start": "pnpm run build && node --enable-source-maps dist/bin.cjs http_server --config_file config.yml",
    "test": "mocha tests",
    "tsc": "tsc",
    "build": "rm -rf dist/ && esbuild cmd/bin.ts --sourcemap --bundle --platform=node --outfile=dist/bin.cjs --target=node12 --loader:.gz=binary --loader:.html=text"
  },
  "devDependencies": {
    "@types/yargs": "^17.0.32",
    "esbuild": "^0.20.2",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "tsx": "^4.16.2",
    "winston": "^3.13.0",
    "yaml": "^2.4.2",
    "yargs": "^17.7.2"
  },
  "engines": {
    "node": ">=16.0"
  }
}
